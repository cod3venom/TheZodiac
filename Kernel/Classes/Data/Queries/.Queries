1$SELECT USER_EMAIL FROM ZODIAC.USERS WHERE USER_ID = ? LIMIT 1;\n
2$INSERT INTO ZODIAC.USERS(USER_ID,USER_EMAIL,USER_PASSWORD,USER_LEVEL) VALUES (?,?,?,?);\n
3$SELECT * FROM ZODIAC.USERS WHERE USER_ID = ? LIMIT 1;\n
4$INSERT INTO ZODIAC.USER_PROFILE (USER_ID,USER_FIRSTNAME, USER_LASTNAME, USER_AVATAR,USER_GENDER, USER_BIRTHDATE) VALUES (?,?,?,?,?,?);\n
5$SELECT * FROM ZODIAC.USER_PROFILE WHERE USER_ID = ? LIMIT 1;\n
6$SELECT * FROM ZODIAC.USERS WHERE USER_ID = ?;\n
7$INSERT INTO ZODIAC.USER_SECURITY (USER_ID, USER_EMAIL, USER_STATUS, USER_RECOVERY, USER_IP, USER_COUNTRY) VALUES (?, ?, ?, ?, ?, ?);\n
8$INSERT INTO ZODIAC.USER_PLAN (USER_ID, USER_PACKET_PRICE, USER_PACKET_OPTION, USER_PACKET_START_DATE, USER_PACKET_END_DATE) VALUES (?, ?, ?, ?, ?);\n
9$SELECT * FROM ZODIAC.USER_PLAN WHERE USER_ID = ? LIMIT 1;\n
10$UPDATE USERS, USER_PROFILE, USER_SECURITY SET USERS.USER_ID = ?, USERS.USER_EMAIL = ?, USERS.USER_PASSWORD = ?, USERS.USER_LEVEL = ?,
USER_PROFILE.USER_ID = ?, USER_PROFILE.USER_AVATAR = ?, USER_PROFILE.USER_NAME = ?, USER_SECURITY.USER_ID = ?, USER_SECURITY.USER_EMAIL = ?,
USER_SECURITY.USER_STATUS = ?, USER_SECURITY.USER_RECOVERY = ?, USER_SECURITY.USER_IP = ?, USER_SECURITY.USER_COUNTRY = ? WHERE USERS.USER_ID = ?;\n
11$INSERT INTO ZODIAC.PLANS (USER_ID,PLAN_TITLE,PLAN_DESCRIPTION,PLAN_PRICE, PLAN_TYPE) VALUES (?, ?, ?, ?, ?);\n
12$SELECT * FROM ZODIAC.PLANS;\n
13$SELECT * FROM ZODIAC.PLANS WHERE USER_ID = ?;\n
14$SELECT * FROM ZODIAC.USER_SECURITY WHERE USER_ID = ?;\n
15$SELECT * FROM ZODIAC.USERS WHERE USER_EMAIL = ? LIMIT 1;\n
